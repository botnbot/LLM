Index: materials/serializers.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>from rest_framework.fields import SerializerMethodField\r\nfrom rest_framework.serializers import ModelSerializer\r\n\r\nfrom materials.models import Course, Lesson\r\n\r\n\r\nclass LessonSerializer(ModelSerializer):\r\n\r\n    class Meta:\r\n        model = Lesson\r\n        fields = \"__all__\"\r\n\r\n\r\nclass CourseSerializer(ModelSerializer):\r\n\r\n    lessons = LessonSerializer(many=True, read_only=True)\r\n    lessons_count = SerializerMethodField()\r\n\r\n    class Meta:\r\n        model = Course\r\n        fields = [\"id\", \"name\", \"preview\", \"description\", \"lessons\", \"lessons_count\"]\r\n\r\n    def get_lessons_count(self, course):\r\n        return course.lessons.count()\r\n\r\n\r\nclass CourseShortSerializer(ModelSerializer):\r\n    class Meta:\r\n        model = Course\r\n        fields = [\"id\", \"name\"]\r\n\r\n\r\nclass LessonDetailSerializer(ModelSerializer):\r\n    count_course_lessons = SerializerMethodField()\r\n    course = CourseShortSerializer()\r\n\r\n    def get_count_course_lessons(self, lesson):\r\n        return lesson.course.lessons.count()\r\n\r\n    class Meta:\r\n        model = Lesson\r\n        fields = [\"name\", \"course\", \"description\", \"count_course_lessons\"]\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/materials/serializers.py b/materials/serializers.py
--- a/materials/serializers.py	(revision ab75e64d16e8e70e144d8bcb613e386e0d7d93ee)
+++ b/materials/serializers.py	(date 1774467678149)
@@ -1,42 +1,29 @@
-from rest_framework.fields import SerializerMethodField
-from rest_framework.serializers import ModelSerializer
-
-from materials.models import Course, Lesson
-
-
-class LessonSerializer(ModelSerializer):
+from urllib.parse import urlparse
+from rest_framework.serializers import ValidationError
 
-    class Meta:
-        model = Lesson
-        fields = "__all__"
 
+class YouTubeValidator:
+    def __init__(self, field):
+        self.field = field
 
-class CourseSerializer(ModelSerializer):
+    def __call__(self, attrs):
+        link = attrs.get(self.field)
 
-    lessons = LessonSerializer(many=True, read_only=True)
-    lessons_count = SerializerMethodField()
+        if not link or not isinstance(link, str):
+            return attrs
 
-    class Meta:
-        model = Course
-        fields = ["id", "name", "preview", "description", "lessons", "lessons_count"]
+        parsed_url = urlparse(link)
+        domain = parsed_url.netloc.lower()
 
-    def get_lessons_count(self, course):
-        return course.lessons.count()
+        allowed_domains = [
+            "youtube.com",
+            "www.youtube.com",
+            "youtu.be",
+        ]
 
+        if not any(domain == d or domain.endswith("." + d) for d in allowed_domains):
+            raise ValidationError({
+                self.field: "Разрешены только ссылки на YouTube"
+            })
 
-class CourseShortSerializer(ModelSerializer):
-    class Meta:
-        model = Course
-        fields = ["id", "name"]
-
-
-class LessonDetailSerializer(ModelSerializer):
-    count_course_lessons = SerializerMethodField()
-    course = CourseShortSerializer()
-
-    def get_count_course_lessons(self, lesson):
-        return lesson.course.lessons.count()
-
-    class Meta:
-        model = Lesson
-        fields = ["name", "course", "description", "count_course_lessons"]
+        return attrs
\ No newline at end of file
